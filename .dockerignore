# VCS
.git
.gitattributes
.gitignore
.github

# Editor / OS
.editorconfig
.vscode
.idea
.DS_Store
Thumbs.db

# Project docs & scripts (not needed in the image)
stf-doc
stf-scripts

# Compose files - only used for local dev, never baked into the image
docker-compose.yml
docker-compose.*.yml
compose.yml
compose.*.yml

# Runtime / generated WordPress dirs
wp-content/uploads
wp-content/cache
wp-content/upgrade

# Secrets
wp-config.php
wp-cli.local.yml

# Node toolchain (in case a JS build is added later)
**/node_modules
